import React from 'react';
import OverviewContainer from '../containers/OverviewContainer.js';

const App = (props) => {
    return (
        <React.Fragment>
            <OverviewContainer />
        </React.Fragment>
    )
};

export default App;